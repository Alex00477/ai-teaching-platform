
# 功能/业务需求：

1.登录时选择用户身份：学生或者老师
2.然后进入登录页面输入用户名、密码、验证码；有注册和忘记密码入口。
3.进入后左侧导航栏有首页、信息、作业、讲义、设置 五部分
（1）学生身份：
首页：agent对话，还包括skill、知识库、MCP功能；可通过对话使agent读取作业情况完成讲解、统计等功能。
信息页：显示注册时所填的个人基本信息，包括所处班级和老师等
作业：选择某科目的作业，完成后保存或直接提交，提交后显示变为已提交状态（作业状态同样在老师端的信息页显示）；作业被批改后，会显示正误情况，每道错题旁边还有按钮，点击以直接使大模型讲解该错题。
讲义：可查看各科目下老师发布的讲义
设置：可设置大模型的型号、API key；设置大模型讲解错题的prompt。
（2）老师身份：
首页：agent对话，还包括skill、知识库、MCP功能；可通过对话使agent读取信息情况完成统计、检索功能，也可使agent批改作业。
信息页：显示注册时所填的科目、班级信息（可支持多个班级，因为一个老师可能交多个班），还包括成员列表，学生作业完成情况等
作业：可查看所有提交给自己的作业，可进行人工批改，也可一键agent批改。
讲义：可上传讲义，讲义会自动入知识库。
设置：可设置大模型的型号、API key；可为大模型设置各种skill、配置MCP。

# 性能需求

1.支持至少500人的同时在线使用
2.作业批改异步处理防止阻塞页面
3.检索时速度要快，比如老师在信息页面搜索某个学生
4.支持缓存，存在近期打开的讲义等数据

# 安全需求

1.APIkey：打码，存服务器
2.数据等都存服务器

## 已确认 / 待确认

### 已确认

- P0 先验证“班级—作业—批改—反馈”教学闭环，再扩展通用 Agent 能力。
- 注册使用邀请码，不在 P0 接入短信验证码；AI 批改按整份作业异步执行，教师确认后才对学生可见。
- 讲义上传后默认未发布，教师明确发布后学生才能查看。
- 模型通过 PH8 的 OpenAI 兼容协议接入，API Key 只由服务端保管。

### 待确认

- 用户是否可以在 P0 配置自己的 API Key；当前技术基线按平台统一的服务端 PH8 Key 设计。
- PH8 的实际限流、上下文长度、超时和计费规则，以及目标服务器的容量和部署权限。
- Agent 中通用 Skill、知识库和 MCP 的开放范围；当前不把任意安装或执行视为 P0 承诺。
- 500 人在线对应的真实请求速率、数据规模和压测口径；性能目标应在服务器和用户规模明确后确定。
