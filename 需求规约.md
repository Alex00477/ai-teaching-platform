
# AI辅助教学平台需求规约（完整版）

## 1. 项目概述

本系统面向教师和学生，提供班级管理、作业布置与提交、AI异步批改、讲义知识库、Agent对话、Skill及MCP配置等功能。系统采用按用户、角色和班级的数据隔离机制，支持多个教师、多个班级和多门课程。

## 2. 用户角色与权限

### 2.1 学生

可查看本人资料、所属班级、教师、讲义和作业；可保存、提交作业，查看批改结果，使用AI讲解错题和学习问答；不可查看其他学生数据、教师配置、未授权讲义及批改规则。

### 2.2 教师

可维护本人负责的多个班级和课程，查看成员及作业情况，发布讲义和作业，人工批改或发起AI批改，修改AI评分并发布结果；可配置本人Agent的模型、Skill、知识库和MCP，但不可访问其他教师的班级数据。

### 2.3 管理员（建议增加）

负责用户审核、角色管理、违规文件处理、系统参数、日志审计和故障处理，不参与日常教学业务。

## 3. 总体业务规则

1. 登录时选择学生或教师身份，身份必须与账号角色一致，否则拒绝登录。
2. 一个学生可加入一个或多个班级；一个教师可管理多个班级；班级与课程建立关联。
3. 所有业务数据必须带有用户、班级或课程归属标识，服务端强制进行权限校验，不能仅依赖前端隐藏页面。
4. 学生只能访问本人数据及其所属班级公开数据；教师只能访问本人负责班级的数据。
5. AI输出仅作为辅助结果，教师拥有最终评分和发布权。

## 4. 功能需求

### 4.1 登录、注册与账号安全

#### 登录

用户选择学生/教师身份后，输入用户名、密码和验证码。登录成功后签发会话或JWT并进入对应首页；连续失败达到阈值后临时锁定或要求增强验证。

#### 注册

学生填写用户名、密码、姓名、学号、邮箱或手机号、班级邀请码等信息；教师填写用户名、密码、姓名、工号、任教科目、邮箱或手机号。用户名、学号和工号须唯一，密码必须满足复杂度要求。教师账号可设置为管理员审核后启用。

#### 找回密码

通过邮箱或手机验证码验证身份后设置新密码；验证码有有效期、次数和频率限制，旧会话可按策略失效。

#### 验收标准

正确账号可登录；身份不匹配时拒绝；错误验证码不能登录；越权访问接口返回403；密码数据库中不得明文保存；退出登录后令牌失效。

### 4.2 学生端

#### 首页Agent

提供对话框、历史会话、新建会话、停止生成和清空会话功能。Agent可在授权范围内读取学生作业状态、错题、讲义知识库并完成学习统计、知识检索、概念讲解和学习计划建议。回答应显示引用的讲义来源；无法确认时明确说明，不得伪造答案。

#### 信息页

展示注册资料、所属班级、任课教师、科目等信息。敏感信息按规则脱敏，学生不能自行修改需要审核的字段。

#### 作业页

按班级、科目和截止时间展示作业。学生可查看题目、附件、要求和截止时间，填写答案或上传文件，保存草稿、修改并提交。提交后状态为“已提交”，截止后按规则禁止修改或标记逾期。提交记录保存版本、时间和文件。

作业状态至少包括：未开始、草稿、已提交、批改中、已批改、已发布、退回修改、批改失败。教师发布成绩后，学生可查看总分、分题得分、评语、正确性和错题讲解按钮；未发布的教师草稿不得被学生看到。

#### 讲义页

按科目、班级和发布时间查看教师发布的讲义，支持在线预览、下载、关键词搜索和最近访问。学生只能查看其班级已发布的讲义。

#### 设置页

可选择系统允许的模型、配置个人API Key和错题讲解Prompt。API Key必须加密存储、前端只显示掩码，日志和错误信息不得泄露；模型和Prompt变更应记录版本及更新时间。

### 4.3 教师端

#### 首页Agent

可查询班级人数、作业提交率、成绩分布、错题统计和讲义内容，并通过Agent进行检索、汇总和教学分析。Agent执行批改等写操作前必须二次确认，并明确作用范围。

#### 信息页

展示教师个人资料、所授科目和多个班级。支持创建班级、生成/作废邀请码、查看成员、移除成员、按姓名/学号搜索学生，以及查看作业完成情况、成绩和逾期情况。搜索结果必须进行权限过滤，并支持分页。

#### 作业页

教师可创建作业，填写标题、科目、班级、说明、题目、分值、截止时间、参考答案、评分标准和允许的提交格式；可保存草稿、发布、撤回和复制作业。

教师可查看所有提交记录，进行人工批改、填写评语、修改分数和发布成绩，也可一键发起Agent批改。AI批改结果必须记录模型、Prompt、知识库版本、时间和原始输出；教师确认后才成为正式成绩。批改失败应显示原因并支持重试或人工处理。

#### 讲义页

教师可上传PDF、Word、PPT、图片等允许格式，限制单文件大小和总容量。上传后进行病毒扫描、文本解析、OCR（必要时）、分段、向量化并写入知识库；处理过程显示排队中、处理中、成功或失败。教师可编辑标题、科目、适用班级、标签和可见范围，发布、撤回或删除讲义。删除或撤回后不得继续被新检索命中，但历史作业批改记录仍保留来源快照。

#### 设置页

教师可配置模型、API Key、温度等允许参数、系统Skill、评分规则、Prompt模板和MCP连接。MCP工具必须登记名称、用途、输入参数、权限范围和风险等级，默认只读；涉及数据修改、发送消息或批量批改时需确认、审计和限流。禁止调用未登记或未授权工具。

## 5. 知识库与Agent需求

1. 文件原件与解析文本分开保存，原件进入对象存储，文本和元数据进入数据库。
2. 知识库检索必须按教师、班级、课程和讲义可见范围过滤，防止跨班级泄露。
3. 支持关键词检索与向量检索，可配置Top-K、相似度阈值和引用来源。
4. Agent区分只读工具和写操作工具；工具调用、模型、Prompt、检索片段和结果均写入审计记录。
5. 对学生隐藏系统Prompt、API Key和教师私有知识库；对模型输出进行长度限制、敏感内容过滤和异常兜底。

## 6. 作业异步批改流程

学生提交后，服务端校验文件和权限，生成批改任务并立即返回“批改中”，页面可通过轮询或WebSocket刷新状态。Worker从队列取任务，读取题目、答案、评分标准和授权知识库，调用模型批改，校验结构化结果并保存分题得分、总分、评语和置信度。失败自动重试，采用退避策略；超过次数后标记“批改失败”并通知教师。教师确认后发布成绩。任务必须幂等，避免重复扣分或重复写入。

## 7. 数据需求

核心实体包括：用户、角色、班级、课程、班级成员、教师班级关系、作业、题目、作业提交、提交版本、批改任务、批改结果、成绩发布记录、讲义、文件、知识库分片、向量、会话、消息、模型配置、Prompt版本、Skill、MCP工具、操作日志和通知。

关键字段应包括主键、创建/更新时间、创建者、所属班级/课程、状态和版本号。用户密码使用强哈希；API Key使用服务器端加密或KMS密钥加密后保存，数据库备份同样加密。文件保存校验值、类型、大小和病毒扫描结果。

## 8. 性能与可用性需求

1. 支持至少500人同时在线，核心查询接口在正常负载下P95响应时间不超过2秒。
2. 登录、作业列表和学生搜索支持分页、索引和必要的全文检索。
3. 作业批改、讲义解析和向量化必须异步执行，不阻塞页面请求。
4. Redis缓存会话、热点讲义、班级统计和检索结果，设置TTL；数据更新后主动失效，不能返回越权或过期成绩。
5. 关键服务异常时应有超时、重试、降级和友好提示；目标可用性不低于99%。
6. 支持断点续传或至少显示大文件上传进度，前端适配电脑和移动浏览器。

## 9. 安全与合规需求

1. 全站HTTPS，密码强哈希，JWT或会话设置有效期、刷新和注销机制。
2. 服务端实施RBAC、对象级权限校验、班级数据隔离和防ID遍历。
3. API Key前端掩码显示、传输加密、服务器加密存储，禁止写入日志、前端源码和错误堆栈；支持删除和轮换。
4. 文件进行扩展名与真实类型校验、大小限制、病毒扫描和安全下载，禁止直接执行上传内容。
5. 防护SQL注入、XSS、CSRF、暴力登录、接口刷量和提示词注入；对Agent工具实行白名单和最小权限。
6. 记录登录、权限变更、文件操作、成绩修改、AI批改和MCP调用日志，日志不可被普通用户删除。
7. 数据定期备份，明确恢复目标；支持数据导出、删除和账号注销，并保护学生个人信息。

## 10. 接口与页面要求

接口统一采用REST/JSON，使用OpenAPI自动生成文档；统一返回码、错误格式、分页格式和幂等请求标识。核心接口包括登录注册、用户资料、班级成员、作业CRUD、提交与批改、讲义上传/发布/检索、Agent对话、模型配置、Skill/MCP配置和日志查询。页面应提供加载、空数据、权限不足、上传失败、AI超时和批改失败等状态。

## 11. 测试与验收

AI开发每个模块都必须同时生成单元测试、接口测试和关键流程测试。验收至少覆盖：不同角色登录、跨班级访问、注册与找回密码、作业保存/提交/逾期、重复提交、人工和AI批改、失败重试、讲义解析与检索、引用来源、缓存失效、500并发、API Key脱敏、MCP越权拦截和备份恢复。所有需求必须对应可执行的验收用例，测试通过后才进入下一模块。

## 12. 交付物与AI开发流程

交付物包括需求规约、原型图、ER图、权限矩阵、接口文档、数据库迁移脚本、源代码、测试报告、部署文件、用户手册、AI对话记录和版本变更记录。开发顺序建议为：项目骨架与认证→班级和权限→作业流程→讲义知识库→异步批改→Agent/Skill/MCP→缓存、监控与部署。AI先分析和拆解，再逐模块实现；每次提交都由开发者按验收标准实际验证并反馈问题，不能一次性生成后直接交付。
